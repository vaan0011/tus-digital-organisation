name: Event Planner – WordPress Playground PR Preview

on:
  pull_request:
    types: [opened, synchronize, reopened, edited]
    paths:
      - "projects/event-planner/plugin/verein-turnierplaner/**"
      - "projects/event-planner/playground/**"
      - ".github/workflows/event-planner-pr-preview.yml"

permissions:
  contents: read
  pull-requests: write

jobs:
  preview:
    name: Event Planner in WordPress Playground
    runs-on: ubuntu-latest
    steps:
      - name: Playground-Vorschau bereitstellen
        uses: WordPress/action-wp-playground-pr-preview@v3
        with:
          blueprint: |
            {
              "$schema": "https://playground.wordpress.net/blueprint-schema.json",
              "landingPage": "/wp-admin/admin.php?page=vtp-dashboard",
              "preferredVersions": {
                "php": "8.2",
                "wp": "7.1"
              },
              "steps": [
                {
                  "step": "installPlugin",
                  "pluginData": {
                    "resource": "git:directory",
                    "url": "${{ github.event.pull_request.head.repo.clone_url }}",
                    "ref": "${{ github.event.pull_request.head.sha }}",
                    "path": "/projects/event-planner/plugin/verein-turnierplaner"
                  },
                  "options": {
                    "activate": true
                  }
                },
                {
                  "step": "setSiteLanguage",
                  "language": "de_DE"
                },
                {
                  "step": "login",
                  "username": "admin"
                }
              ]
            }
          github-token: ${{ secrets.GITHUB_TOKEN }}
